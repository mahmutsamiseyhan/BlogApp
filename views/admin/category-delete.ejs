<!DOCTYPE html>
<html lang="tr">
<head>
    <%- include("../partials/head") %> 
</head>
<body>
    
    <%- include("../partials/nav") %> 

    <div class="container mt-3">
        <div class="row">
            <div class="col-md-12 mt-3 text-center">
                <!-- Silme Onay Mesajı -->
                <div class="alert alert-secondary">
                    <p class="mb-3">
                        <b><%= category.name %></b> isimli kategoriyi silmek istiyor musunuz?
                    </p>
                    <form method="post">
                        <!-- CSRF Koruma -->
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <!-- Silinecek Kategori ID'si -->
                        <input type="hidden" name="categoryid" value="<%= category._id %>">
                        <!-- Onay ve İptal Butonları -->
                        <button class="btn btn-danger" type="submit">Evet</button>
                        <a href="/admin/categories" class="btn btn-primary">Vazgeç</a>
                    </form>
                </div>
            </div>
        </div>
    </div>

</body>
</html>
